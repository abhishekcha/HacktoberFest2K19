

#include<stdio.h>

int main()
{
    int N,a,i,r;
	scanf("%d", &N);
	long data[100000];
	for(i=0; i<N; i++)
	    scanf("%ld ", &data[i]);
	    for(i=0;i<N;i++)
	    {
	    r=data[i]%10;
	   // if(a%)
	    //printf("%d",r);
	    //if(r%10)
	    a=data[i]/10;
	    }
	    if(r%10==0)
	    printf("Yes");
	    else
	    printf("No");
}
